#include <stdio.h>
/**
 * mul - Works for multiplying
 * @a: check fot int
 * @b: check for int
 *
 * Description: Maybe works
 * Return: 0 (failure) or 1 (success)
 */
int mul(int a, int b)
{
	int sol = a * b;

	return (sol);
}
