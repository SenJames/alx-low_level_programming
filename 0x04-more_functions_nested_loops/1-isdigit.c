#include <stdio.h>
/**
 * _isdigit - Works for checking upper
 * @c: check fot int
 *
 * Description: Maybe works
 * Return: 0 (failure) or 1 (success)
 */
int _isdigit(int c)
{
	if (c >= 48 && c <= 57)
	{
		return (1);
	}
	else
	{
		return (0);
	}
	return (0);
}
