#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
   char res[] = "Programming is like building a multilingual puzzle\n";
   
   puts(res);

   return(0);
}
