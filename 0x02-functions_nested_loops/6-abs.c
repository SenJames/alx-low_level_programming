#include <stdio.h>
#include <stdlib.h>
#include "main.h"
/**
 * _abs - Works for checking upper
 * @c: check fot int
 *
 * Description: Maybe works
 * Return: 0 (failure) or 1 (success)
 */
int _abs(int c)
{
	int val;

	val = abs(c);
	return (val);
}
